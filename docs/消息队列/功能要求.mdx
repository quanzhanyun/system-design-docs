---
sidebar_position: 1
---

# 功能要求

- Worker 根据厂商和功能的组合单独成立，并且可独立启动、停止。例如，处理上游厂商（百度云）的 CDN 域名队列可以单独启动、停止，也可以在多个服务器上同时启动、停止。
- Worker 支持接收结束信号（例如 SIGTERM）并处理完当前的任务后停止。
- 每个 Worker 都运行在单独进程中，并且同一个 Worker 可以支持启动多个实例。[参考资料](https://blog.taskforce.sh/using-typescript-with-bullmq/)。